This folder is intended to contain image assets referenced by the project README.

Please add the sensor setup image file as:

- `images/iot_setup.png`

If you have the original image from the project attachments, copy it into this path so the README will display it correctly.

Alternative: if you prefer to keep large assets out of the repo, host the image externally and update the README to point to that URL.